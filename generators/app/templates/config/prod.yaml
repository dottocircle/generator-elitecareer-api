---
  requiredHeaders:
    - fusion-api-key
    - authorization
    - content-type
  headerValues:
    fusion-api-key:
      - key
      - key
      - key
    content-type:
      - application/json
  db:
    elitecareer_usa:
      host: localhost
      username: username
      password: password
    elitecareer_list:
      host: localhost
      username: username
      password: password

